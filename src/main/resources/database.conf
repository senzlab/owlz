db {
  mongo {
    host = "dev.localhost"
    host = ${?MONGO_HOST}
    port = 27017
    port = ${?MONGO_PORT}
    db-name = "owl"
    db-name = ${?DB_NAME}
    coll-name = "owls"
    coll-name = ${?COLL_NAME}
  }
}

